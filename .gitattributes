# Normalise line endings to LF for all text files
# Prevents CRLF/LF warnings on Windows and keeps the repo consistent cross-platform

* text=auto eol=lf

# Explicitly mark binary files so Git never touches their line endings
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.ico binary
